{
  "name": "renate-transcription-bot",
  "private": true,
  "version": "0.0.0",
  "description": "Self-hosted Google Meet transcription bot (scaffold)",
  "workspaces": [
    "bot",
    "worker",
    "api"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
